import 'package:flutter/material.dart';

class DetayliBilgiSayfasi extends StatelessWidget {
  const DetayliBilgiSayfasi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Detayli Bilgi Sayfasi")),
    );
  }
}
